export type TFormValues = {
  fullName: string
  email: string
  telephone: string
}

export type TModalContainer = {
  title: string
  paragraph: string
  submitButton: string
  toggleModal: () => void
}
